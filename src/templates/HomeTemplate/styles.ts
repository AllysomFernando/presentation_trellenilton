import styled from "styled-components";

export const S = {
	Container: styled.div`
		display: flex;
		flex-direction: row;
		justify-content: center;
	`,
	Main: styled.div`
		display: flex;
		width: 100%;
	`,
	Header: styled.header``,
};
